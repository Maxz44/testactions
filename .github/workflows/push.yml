name: on push

on:
  workflow_dispatch:
    inputs:
      name:
        description: a name
        default: max
        

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - run: echo -${{ inputs.name }}-